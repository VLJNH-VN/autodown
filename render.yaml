services:
  - type: web
    name: downall-api
    runtime: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg python3-pip
      pip install yt-dlp
      npm install
    startCommand: npm start
